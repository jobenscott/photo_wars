<div class="container">
<h1> Image </h1>
<h2><%= @image.title %></h2>
<h3><%= image_tag @image.url %></h2>
<p><%= @image.vote %></p>
<p><%= @image.description %></p>
<%= link_to 'Back', images_path %>


<h3> Comments </h3>
<% @comments.each do |comment| %>
  <p> <%= comment.body %> </p>
 
 <%end%> 

<%= render 'comments/form'  %>


</div>
